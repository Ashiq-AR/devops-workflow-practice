{{- if .Values.isOpenShift }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "frontend.fullname" . }}
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
spec:
  to:
    kind: Service
    name: {{ include "frontend.fullname" . }}
    weight: 100
  port:
    targetPort: http-notesapp-frontend
{{- end }}
